const inquirer = require('inquirer');
const fs = require('js')
const generateHTML = require("./generateHTML");

//init function to gather data

//function that feeds data to HTML to generate correct blocks
